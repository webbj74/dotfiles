# Laptop Config

```bash
# Use "Self Service" to install iterm2

# Use "Self Service" to install Google Filestream

# Install Homebrew
$ /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

# Install phpstorm & command-line tool

```
